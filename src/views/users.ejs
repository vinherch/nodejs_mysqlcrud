<table class="table table-sm table-striped table-hover mt-5">
  <thead>
    <tr>
      <th scope="col">ID</th>
      <th scope="col">Email</th>
      <th scope="col">Firstname</th>
      <th scope="col">Lastname</th>
      <th scope="col">Type</th>
      <th scope="col">Created</th>
      <th scope="col">Updated</th>
    </tr>
  </thead>
  <tbody>
    <%if (Array.isArray(result)) { for (rec of result) { %> <% %>
    <tr class="record">
      <th class="user-id"><%= rec.id%></th>
      <td class="user-email"><%= rec.email%></td>
      <td contenteditable="true" class="user-fn"><%= rec.firstname%></td>
      <td contenteditable="true" class="user-ln"><%= rec.lastname%></td>
      <td class="user-type"><%= rec.user_type%></td>
      <td class="user-created"><%= rec.created%></td>
      <td class="user-updated"><%= rec.updated%></td>
      <td>
        <a href="/users/create" class="btn btn-primary btn-create">Create</a>
      </td>
      <td>
        <button class="btn btn-primary btn-update">Update</button>
      </td>
      <td>
        <button class="btn btn-secondary btn-delete">Delete</button>
      </td>
    </tr>
    <% }} else { %>
    <p><%-result%></p>
    <%}%>
  </tbody>
</table>
